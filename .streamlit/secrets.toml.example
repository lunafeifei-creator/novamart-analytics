# Streamlit Cloud Secrets Configuration Template
# Rename this file to secrets.toml and fill in your actual values
# This file should NEVER be committed to GitHub

# Database Configuration (if using database)
# [database]
# host = "your-db-host"
# user = "your-username"
# password = "your-password"
# database = "your-database"

# API Keys (if needed for external services)
# [api]
# openai_key = "sk-..."
# anthropic_key = "sk-ant-..."

# Example for Streamlit Cloud:
# 1. Go to your app settings on streamlit.io/cloud
# 2. Click "Secrets" tab
# 3. Paste your secrets here (without the # comments)
# 4. In your code, access with: st.secrets["key"]

# Example usage in code:
# ```python
# import streamlit as st
# db_password = st.secrets["database"]["password"]
# ```
